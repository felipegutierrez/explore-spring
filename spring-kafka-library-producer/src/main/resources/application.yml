spring:
  profiles:
    active: dev
---
topic:
  name: library-events
  partitions-num: 1
  replication-factor: 1
spring:
  config:
    activate:
      on-profile: dev
  kafka:
    template:
      default-topic: library-events
    producer:
      bootstrap-servers: localhost:9092,localhost:9093,localhost:9094
      key-serializer: org.apache.kafka.common.serialization.IntegerSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      # value-serializer: io.confluent.kafka.serializers.json.KafkaJsonSchemaSerializer
      # bootstrap-servers: localhost:9092
      properties:
        acks: all
        retries: 10
        retry.backoff.ms: 1000
    properties:
      schema.registry.url: http://127.0.0.1:8081
    admin:
      properties:
        bootstrap.servers: localhost:9092,localhost:9093,localhost:9094
        # bootstrap.servers: localhost:9092
---
spring:
  config:
    activate:
      on-profile: devssl
  kafka:
    template:
      default-topic: library-events
    producer:
      bootstrap-servers: localhost:9095,localhost:9096,localhost:9097
      key-serializer: org.apache.kafka.common.serialization.IntegerSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      ssl:
        trust-store-location: file:/home/felipe/workspace-idea/explore-spring/spring-kafka-library-producer/ssl/client.truststore.jks
        trust-store-password: password
        key-store-location: file:/home/felipe/workspace-idea/explore-spring/spring-kafka-library-producer/ssl/client.keystore.jks
        key-store-password: password
      properties:
        acks: all
        retries: 10
        retry.backoff.ms: 1000
        security:
          protocol: SSL
        ssl.endpoint.identification.algorithm:

---
spring:
  config:
    activate:
      on-profile: prod
  kafka:
    producer:
      bootstrap-servers: prod:9092
      key-serializer: org.apache.kafka.common.serialization.IntegerSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
